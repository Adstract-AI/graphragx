# graphragX

`graphragX` is a framework for exploring graph-aware retrieval-augmented generation.

The project focuses on combining graph-structured retrieval with LLM-based reasoning in a modular and experiment-friendly way.
